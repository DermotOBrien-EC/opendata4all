{
  "name": "opendata4all",
  "version": "0.1.0",
  "private": true,
  "description": "Local-first toolkit for voluntary AI interaction data donation.",
  "license": "Apache-2.0",
  "bin": {
    "od4a": "./bin/od4a.mjs"
  },
  "scripts": {
    "validate": "npm run validate:schemas && npm run validate:examples && npm run validate:secrets && npm run validate:cli && npm run validate:templates",
    "validate:schemas": "node scripts/check-schemas.mjs",
    "validate:examples": "node scripts/check-examples.mjs",
    "validate:secrets": "node scripts/check-repo-secrets.mjs",
    "validate:cli": "node scripts/check-cli.mjs",
    "validate:templates": "node scripts/check-policy-templates.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
